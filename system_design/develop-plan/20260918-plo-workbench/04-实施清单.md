# 04. 实施清单（开发入口）

> 本文件是正式编码的入口。流程以 [02-目标流程.md](./02-目标流程.md) 为准；契约以 [03-schema设计.md](./03-schema设计.md) 为准。  
> **无 Liquibase。**

---

## 1. 审查结论

方向正确，可以开工：这是把生产转单作业对齐采购工作台 + 销售订单智能批量，**不是**改 MRP 算法，也不是算完自动转单。

| 问题 | 结论 |
|------|------|
| 会不会变成计算后自动转 PLO | **否**。待转仍要人勾选提交 |
| 要不要跳过 PLO 出 MO | **否** |
| 结果页是否保留「转计划单」POST | **否**，只跳转，与 PURCHASE 对称 |
| 旧 `convert-to-planned-orders` | **删除**，只留 `from-mrp-results` |
| 待转是否跨历史 Run | **否**，与采购同一套最新 Run |
| `hasProtectedOrder` 静默 skip | **改为**列表禁用 + 提交整批失败 |
| firm/unfirm 静默忽略 | **改为**整批校验失败 |
| 转 MO 是否必须先 FIRMED | **否**，维持现网 PROPOSED 可转；只改 tooltip |
| PROPOSED+FIRMED 混选转 MO | **允许** |
| 新权限 / 新菜单 | **否** |
| 与采购工作台共用组件 | **否**（无供应商/部分转）；只抄 PageContainer Tab 与最新 Run 查询 |
| Tab 是否抄采购 `useState(workspace)` | **否**。从 URL 派生，否则结果页跳转会脱节 |
| 空选时 `every` 算出可转 MO / 可删 | **否**。必须先 `hasSelected` |
| 待转分页逐行查保护 | **否**。当前页批量 Pegging 再判 |
| `from-mrp-results` 加锁顺序 | **id 升序**；最新 Run 按厂缓存 |

未改业务代码。编码从 §5 按模块推进。

---

## 2. 已锁定决策

| # | 决策 | 值 |
|---|------|-----|
| 1 | 单据链 | `MrpResult PRODUCTION → PLO → MO` 不变 |
| 2 | 计划员转 PLO 入口 | `/pp/planned-orders?tab=mrp`；结果页只跳转 |
| 3 | 结果页 PRODUCTION 权限 | 无 `/pp/planned-orders:update` 则跳转按钮禁用 |
| 4 | 待转池 | 每厂最新 `COMPLETED+OPERATIVE` Run 的 OPEN 生产建议 |
| 5 | 部分转 / 工作台改量 | **否** |
| 6 | 保护行 | 不可勾选；提交含保护行则整批失败 |
| 7 | 转单返回 | `List<PlannedOrderResponse>` |
| 8 | PLO 行内状态动作 | **移除**；工具栏智能批量 |
| 9 | 行内保留 | 编辑、Pegging |
| 10 | firm/unfirm API | 非法状态整批失败，不再静默 |
| 11 | 手建 PLO | **否** |
| 12 | 培训 | 同步改 PB-04 |
| 13 | Tab 状态 | 从 `searchParams.get('tab')` 派生，不另存 `useState` |
| 14 | 待转单位 | 物料基本单位只读展示；不可改 |
| 15 | 待转查询归属 | `MrpService.findPendingProductionItems`；Controller 挂在 planned-orders |
| 16 | Pegging 主键 | `sourceId = mrpResultId` |

---

## 3. 实现约定

### 3.1 最新 Run 与加锁

复用 `MrpServiceImpl.findLatestCoveringOperativeRunId` / `findCoveringOperativeRuns`，**不要**复制第三套定义。待转分页的 subquery 与 `findPendingPurchaseItems` 同构，仅动作类型与状态不同。

`from-mrp-results`：

1. `mrpResultIds` **升序**后再 `findByIdForUpdate`（防交叉勾选死锁）
2. 与采购 `lockAndValidateForConvert` 对等的生产校验（PRODUCTION、最新 Run、remaining>0、非保护）
3. 同事务 `Map<Long, Long>` 按 `factoryId` 缓存最新 Run，跨厂一批提交时也不要每行重查

### 3.2 保护行

待转分页出数后，对当前页计算 `convertible` / `blockReason`。  
`hasProtectedOrder` 的 FO 来自 `OrderPegging demandType=FACTORY_ORDER supplyType=MRP_RESULT`。无 FO pegging 的组件层建议：视为可转（与现网循环行为一致：只在 FO peg 上检查保护）。

必须批量：`findBySupplyTypeAndSupplyIdIn` 一次取出本页 pegging，内存分组后再判保护。禁止逐行 exists。契约见 [03](./03-schema设计.md) §4。

### 3.3 前端批量启用

不要只抄销售订单的「全部同一状态」。PLO 用行规则聚合。  
**空数组 `.every()` 为 true**，转 MO / 删除必须先挡空选：

```text
hasSelected  = (selectedRows?.length ?? 0) > 0
allProposed  = hasUniformStatus(..., 'PROPOSED')
allFirmed    = hasUniformStatus(..., 'FIRMED')
canConvertMo = hasSelected && selectedRows.every(r => getPlannedOrderRowActions(r).canConvertToMo)
canDelete    = hasSelected && selectedRows.every(r => getPlannedOrderRowActions(r).canDelete)
```

`ExecuteButton.disabled` / `tooltipId` 按上表；execute 内仍 `assertBulkStatus`（仅 firm/unfirm）或手写 `canConvertMo` / `canDelete` 失败提示。  
`BatchDeleteButton` 在 `!canDelete` 时也要 `disabled`，并用 `bulkDeleteConvertedForbidden` 作 tooltip。

`tableAlertRender` **打开**（现网是 `false`）。

### 3.3.1 计划工单页 Tab

```text
const currentTab = resolveWorkspace(searchParams.get('tab'))
```

不要 `useState(() => resolveWorkspace(...))`。`onTabChange` 只改 URL；结果页 `history.push(?tab=mrp)` 必须立刻切到待转。

### 3.4 目录

按前端规范拆：

```text
pages/pp/planned-orders/
  index.tsx
  columns/
    index.ts
    plannedOrderMain.tsx      // 现 columns.tsx
    mrpProductionMain.tsx
  components/
    index.ts
    PlannedOrderForm.tsx
    MrpProductionWorkbench.tsx         // rowKey=mrpResultId；Pegging 用 mrpResultId
    PlannedOrderMoreActionsButton.tsx  // 仅 Pegging，或删除改 buildActions
  utils/plannedOrderActions.ts
```

### 3.5 i18n（三语）

前端（`en-US` / `vi-VN` / `zh-CN`）：

| key | 用途 |
|-----|------|
| `pages.pp.plannedOrders.tab.orders` | Tab「计划工单」 |
| `pages.pp.plannedOrders.tab.mrp` | Tab「MRP 待转」 |
| `pages.pp.plannedOrders.mrpWorkbench.title` | 待转表头 |
| `pages.pp.plannedOrders.mrpWorkbench.convert` | 转计划工单 |
| `pages.pp.plannedOrders.mrpWorkbench.convertConfirmTitle` | 确认标题 |
| `pages.pp.plannedOrders.mrpWorkbench.convertConfirmContent` | 确认内容，带 `{count}` |
| `pages.pp.plannedOrders.mrpWorkbench.convertSuccess` | 成功，带单号 |
| `pages.pp.plannedOrders.mrpWorkbench.protected` | 保护行 tooltip / 列 |
| `pages.pp.plannedOrders.mrpWorkbench.unit` | 基本单位列 |
| `pages.pp.plannedOrders.bulkFirmProposedOnly` | 确认只吃 PROPOSED |
| `pages.pp.plannedOrders.bulkUnfirmFirmedOnly` | 取消确定只吃 FIRMED |
| `pages.pp.plannedOrders.bulkConvertMakeOpenOnly` | 转 MO：非 MAKE 或含 CONVERTED |
| `pages.pp.plannedOrders.bulkDeleteConvertedForbidden` | 删除含 CONVERTED |
| `pages.pp.mrpRuns.results.goToPlannedOrderWorkbench` | 结果页跳转 |
| `pages.pp.plannedOrders.convertToMo.tooltip` | **修正**：PROPOSED 或 FIRMED 均可 |
| `pages.pp.mrpRuns.results.filter.all.tip` | **修正**：选类型以进入对应工作台 |

后端（`messages.properties` / `zh_CN` / `vi`），key 已锁：

| key | 中文参考 |
|-----|----------|
| `validation.mrpResult.not.production` | 只有生产建议的 MRP 结果可以转计划工单 |
| `validation.mrpResult.convert.protected.plo` | 需求已有锁定或已转生产的计划工单，不可重复转换 |
| `validation.plannedOrder.firm.only.proposed` | 只有建议状态的计划工单可以确认 |
| `validation.plannedOrder.unfirm.only.firmed` | 只有确定状态的计划工单可以取消确定 |

### 3.6 测试

- `convertToPlannedOrders`：最新 Run 才转；旧 Run 400；保护行 400；成功建 PLO+pegging+CONVERTED
- 待转分页：只含最新 Run PRODUCTION OPEN；保护行 `convertible=false`；组件层无 FO peg 仍 `convertible=true`
- firm：混入 FIRMED → 失败且库不变
- 前端：结果页 PRODUCTION 无 convert POST（单测或 e2e 能做再做；至少手工验收）
- 手工：空选时转 MO / 删除禁用；`?tab=mrp` 深链切到待转

现网若无 `MrpConversionService` 测试，本波补关键路径，不要只靠 UI。

---

## 4. 改动面

| 模块 | 文件（预期） | 做什么 |
|------|----------------|--------|
| PP API | `PlannedOrderController` | pending-page + from-mrp-results |
| PP | `MrpController` | 删除 convert-to-planned-orders |
| PP | `MrpConversionServiceImpl` | id 升序加锁、厂级 Run 缓存、保护失败、返回 PLO 列表 |
| PP | `MrpServiceImpl` | `findPendingProductionItems`（查询 + 批量 convertible）；conversion 复用其最新 Run |
| PP | `PlannedOrderServiceImpl` | firm/unfirm 整批校验 |
| FE | `planned-orders/*` | Tab + 工作台 + 智能批量 |
| FE | `ResultList.tsx` | 跳转；去掉 PRODUCTION 勾选转单 |
| FE | `endpoints.ts` / planned-order api | 新路径；删旧 convert |
| i18n | pages + messages | §3.5 |
| 培训 | `PB-04-mrp-to-production.md` | 结果页跳转；PLO 页两 Tab；批量转 MO |

不改：`MrpCalculationEngine` 净算与清理、采购工作台、MO 展开 BOM 逻辑（除调用方仍走 `convertToProductionOrders`）。

---

## 5. 提交顺序

1. 后端：pending 查询 + from-mrp-results（含校验）+ 删除旧 convert 端点 + firm/unfirm 收紧 + 测试  
2. 前端：PLO 页 Tab 骨架 + 待转工作台  
3. 前端：orders Tab 智能批量；行内动作收缩  
4. 前端：ResultList 跳转  
5. i18n 三语 + PB-04  

前后端可按 1→2 再 3→4 分两次提交，但旧 convert 端点必须与 ResultList 去掉 POST **同一交付**，避免窗口期无入口。

---

## 6. 验收

- [ ] `/pp/planned-orders` 两个 Tab；`?tab=mrp&factoryId=` 深链可用（从结果页跳转不必刷新整页也能切 Tab）  
- [ ] 空选时确认 / 取消确定 / 转 MO / 删除全部禁用  
- [ ] 待转只显示最新 OPERATIVE Run 的 OPEN 生产建议  
- [ ] 保护行不可勾；强行提交 400，无新 PLO  
- [ ] 转出后待转消失，`orders` 出现 PROPOSED；pegging 可从两边打开  
- [ ] 勾选多条 PROPOSED：确认、转 MO、删除均可一次完成  
- [ ] 勾选 PROPOSED+FIRMED：确认/取消确定禁用，转 MO 成功  
- [ ] 勾选含 CONVERTED：checkbox 不可勾（现网）  
- [ ] 非法 firm 返回错误且状态不变  
- [ ] MRP 结果 PRODUCTION 只跳转，网络无 convert-to-planned-orders  
- [ ] PURCHASE 跳转采购工作台仍可用  
- [ ] 删 PROPOSED PLO 后，若仍是最新 Run，行回到待转  
- [ ] 再跑 MRP：PROPOSED 被清，FIRMED 仍在并占供给  
- [ ] PB-04 与界面一致  

浏览器：待转转出 → 切 orders 批量转 MO → 打开 MO 能看到来源计划单（现网字段）。无浏览器工具时用 API + 页面手工清单，并在交付说明里写明未自动化的点击路径。
