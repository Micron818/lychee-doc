1. 浏览器加载 HTML 和 JS 文件
   ↓
2. Umi MAX 运行时初始化
   ↓
3. 插件系统启动
   ↓
4. @umijs/plugin-initial-state 插件检测到 getInitialState 导出
   ↓
5. 自动调用 getInitialState() 函数 ← 这里！
   ↓
6. 等待 getInitialState() 完成并获取返回值
   ↓
7. 将返回值存储到全局状态中
   ↓
8. 继续执行其他插件（layout、access 等）
   ↓
9. 开始渲染应用