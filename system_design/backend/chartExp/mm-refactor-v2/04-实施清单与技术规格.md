# 04. 实施清单与技术规格（开发入口）

---

## 1. 实施波次与任务分解

```text
┌────────────────────────────────────────────────────────┐
│                   V2 实施路线图                         │
├─────────────┬──────────────────────────────────────────┤
│ 波次 1      │ 后端基础设施：DDL、策略工厂、策略解析器     │
├─────────────┼──────────────────────────────────────────┤
│ 波次 2      │ 后端业务层：持久化前自动发号、统一防重校验 │
├─────────────┼──────────────────────────────────────────┤
│ 波次 3      │ 前端瘦身：MaterialForm 策略自适应、Hook 抽离│
├─────────────┼──────────────────────────────────────────┤
│ 波次 4      │ 管理端增强：分类维护抽屉支持配置策略与流水  │
└─────────────┴──────────────────────────────────────────┘
```

| 波次 | 模块 | 核心工作清单 | 可否独立上线 |
| :---: | :--- | :--- | :---: |
| **波次 1** | 后端基础 | 1. 执行 `material_categories` DDL 添加策略与流水配置列（`code_strategy`, `code_prefix`, `seq_length`, `date_format`, `is_seq_shared`）及 CHECK 约束<br/>2. 增加 `CodingStrategyEnum`<br/>3. 扩展 `MaterialCategoryRequest` 与 `MaterialCategoryResponse` DTO 字段并在分类 CRUD 服务中实现映射与校验<br/>4. 实现分类树策略递归解析算法（`resolvePolicy`，支持逐级继承、号池属主推导与号池作用域计算）<br/>5. 提供 `/api/mm/material-categories/{id}/effective-policy` 接口并在分类树响应中扩展策略字段 | **可以** |
| **波次 2** | 后端业务与排他解耦 | 1. 抽象 `MaterialCodeGenerator` 接口与 `MaterialCodeGeneratorFactory`<br/>2. 实现 `FashionVariantCodeGenerator`（门面委托 `MaterialVariantService`，单笔算号并落库流水）<br/>3. 实现 `SequentialCodeGenerator`（基于 `sys_doc_sequence` 原子自增发号并支持碰撞重试）与 `ManualCodeGenerator`<br/>4. 改造 `MaterialRequest.code` 校验规则（降级为可空，由策略动态校验必填）<br/>5. `MaterialServiceImpl.createMaterial` 与 `updateMaterial` 策略路由接入（创建发号、更新不可变且免发号）<br/>6. `generateVariants` 增加目标分类非变体策略 400 强拦截<br/>7. **同步执行 DDL 移除 `materials` 上的两条 V1 物理变体唯一索引** | 依赖波次 1 |
| **波次 3** | 前端重构 | 1. 分类管理抽屉（`MaterialCategoryForm.tsx`）增加策略、前缀、位数、共用号池配置表单项<br/>2. `MaterialForm` 接入策略监听，动态切换 `code` 只读态及款式/色码维度的显隐<br/>3. 物料列表复制操作（`handleCopy`）适配多策略清空编码<br/>4. 将变体预览与建议逻辑抽离为 `useMaterialVariantSuggest` Hook | 依赖波次 2 |
| **波次 4** | 向导强化与导入兼容 | 1. 变体矩阵向导（`VariantGenerateDrawer`）第三步选择分类时增加非变体分类前端拦截<br/>2. Excel 批量导入及集成接口统一接入 `MaterialCodeGeneratorFactory` 发号与校验 | 依赖波次 3 |

---

## 2. 接口设计与技术规格（API Contracts）

### 2.1 获取物料分类的有效编码策略
* **端点**：`GET /api/mm/material-categories/{id}/effective-policy`
* **说明**：自动执行向上递归继承，返回该分类实际生效的编码策略。
* **响应报文示例**：
  ```json
  {
    "success": true,
    "data": {
      "categoryId": 105,
      "categoryCode": "TSHIRT",
      "strategy": "SEQUENTIAL",
      "sourceCategoryId": 10,
      "sourceCategoryName": "针织上衣",
      "codePrefix": "FAB",
      "seqLength": 5,
      "dateFormat": null,
      "isSeqShared": false,
      "seqScopeCategoryId": 105
    }
  }
  ```

---

### 2.2 物料保存与批量生成接口规格适配

#### A. 单笔物料保存接口（`POST /api/mm/materials`）
* **接口契约调整**：
  - DTO `MaterialRequest.code` 字段移除非空约束（允许前端传空），保留 `@Size(max = 50)`；
  - 必填性由业务策略层校验：
    - `SEQUENTIAL`：允许前端传空 `code`，由 `SequentialCodeGenerator` 原子发号并写入，接口响应实体返回正式生成的 `code`；若用户显式手填了 `code`，系统校验其唯一性并原样采用；
    - `FASHION_VARIANT`：前端通过款式色码动态回填建议编码，保存时由 `FashionVariantCodeGenerator` 严格核验公式并写入流水；
    - `MANUAL`：前端必填 `code`，后端校验唯一性与长度。

#### B. 变体向导批量生成接口（`POST /api/mm/materials/variant-generate`）
* **强安全拦截**：
  - 后端在接收到请求时，首先根据 `request.getCommon().getMaterialCategoryId()` 调用分类策略解析服务；
  - 若 `policy.getStrategy() != CodingStrategyEnum.FASHION_VARIANT`，直接抛出 400 业务异常（`validation.material.variant.generate.notFashionVariantCategory`），彻底杜绝非变体物料被向导批量生成。

---

## 3. 后端类结构与核心设计

```text
com.lychee.erp.mm
  ├── enums
  │     └── CodingStrategyEnum.java
  ├── dto
  │     └── response
  │           └── EffectiveCodingPolicyResponse.java
  ├── strategy
  │     ├── MaterialCodeGenerator.java                 <-- 核心策略接口
  │     ├── MaterialCodeGeneratorFactory.java          <-- 策略路由工厂
  │     └── impl
  │           ├── FashionVariantCodeGenerator.java     <-- 门面适配器: 委托 MaterialVariantService
  │           ├── SequentialCodeGenerator.java         <-- 封装分类前缀原子流水号
  │           └── ManualCodeGenerator.java             <-- 封装手工录入校验
  └── service
        └── impl
              ├── MaterialCategoryServiceImpl.java     <-- 包含向上继承与号池解析
              └── MaterialServiceImpl.java             <-- 保存/更新时路由调用
```

### 3.1 核心接口签名

```java
public interface MaterialCodeGenerator {
    /**
     * 所支持的策略枚举
     */
    CodingStrategyEnum getStrategy();

    /**
     * 在落库前分配/确定正式的物料编码 (仅在新增时调用，更新时严禁调用)
     * @param request 物料入参
     * @param policy 当前生效的策略配置
     * @return 最终确定的物料编码
     */
    String allocateCode(MaterialRequest request, EffectiveCodingPolicy policy);

    /**
     * 输入参数合法性与业务规则校验
     * @param existing 原物料实体 (新增时为 null，更新时非空)
     * @param request 待保存的入参
     * @param policy 当前生效的策略配置
     */
    void validate(Material existing, MaterialRequest request, EffectiveCodingPolicy policy);
}
```

### 3.2 序列号发号器（`SequentialCodeGenerator`）实现要点

```java
@Service
@RequiredArgsConstructor
public class SequentialCodeGenerator implements MaterialCodeGenerator {

    private final SysDocSequenceRepository sequenceRepository;
    private final MaterialRepository materialRepository;

    @Override
    public CodingStrategyEnum getStrategy() {
        return CodingStrategyEnum.SEQUENTIAL;
    }

    @Override
    @Transactional(propagation = Propagation.REQUIRES_NEW)
    public String allocateCode(MaterialRequest request, EffectiveCodingPolicy policy) {
        // 1. 若手填了编码，直接使用手填编码 (手填编码在 validate 中已完成防重与格式校验)
        if (StringUtils.hasText(request.getCode())) {
            return request.getCode().trim();
        }

        Long tenantId = CurrentTenant.tenantId();
        String seqKey = "MATERIAL_CAT:" + policy.getSeqScopeCategoryId();
        int length = policy.getSeqLength() != null ? policy.getSeqLength() : 5;
        String prefix = StringUtils.hasText(policy.getCodePrefix()) ? policy.getCodePrefix() : "MAT";
        String datePart = "";
        if (StringUtils.hasText(policy.getDateFormat())) {
            datePart = LocalDate.now().format(DateTimeFormatter.ofPattern(policy.getDateFormat())) + "-";
        }

        // 2. 循环自增取号 (最多重试 3 次，防止手工输入料号偶然占用了未来流水号)
        for (int retry = 0; retry < 3; retry++) {
            Long nextVal = sequenceRepository.incrementAndGetSequence(seqKey, tenantId);
            String paddedSeq = String.format("%0" + length + "d", nextVal);
            String candidateCode = prefix + "-" + datePart + paddedSeq;

            if (candidateCode.length() > 50) {
                throw new ValidationException("validation.material.code.overflow");
            }
            if (!materialRepository.existsByCode(candidateCode)) {
                return candidateCode;
            }
        }

        throw new ConflictException("validation.material.code.duplicate");
    }

    @Override
    public void validate(Material existing, MaterialRequest request, EffectiveCodingPolicy policy) {
        if (StringUtils.hasText(request.getCode())) {
            if (request.getCode().length() > 50) {
                throw new ValidationException("validation.material.code.size");
            }
            Long excludeId = existing != null ? existing.getId() : null;
            boolean duplicate = excludeId == null
                    ? materialRepository.existsByCode(request.getCode())
                    : materialRepository.existsByCodeAndIdNot(request.getCode(), excludeId);
            if (duplicate) {
                throw new ValidationException("validation.material.code.duplicate");
            }
        }
    }
}
```

### 3.3 变体策略门面（`FashionVariantCodeGenerator`）实现要点

```java
@Service
@RequiredArgsConstructor
public class FashionVariantCodeGenerator implements MaterialCodeGenerator {

    private final MaterialVariantService materialVariantService;

    @Override
    public CodingStrategyEnum getStrategy() {
        return CodingStrategyEnum.FASHION_VARIANT;
    }

    @Override
    public String allocateCode(MaterialRequest request, EffectiveCodingPolicy policy) {
        // 委托变体服务：分配款×色×尺码流水 Token 并拼接标准变体编码 (如 A12345-A01)
        return materialVariantService.resolveOrAllocateCode(request);
    }

    @Override
    public void validate(Material existing, MaterialRequest request, EffectiveCodingPolicy policy) {
        // 委托变体服务：校验款号、尺码组、本款颜色、模式锁定以及变体查重 (传入 existing 支持自我排除)
        materialVariantService.validateAndAllocateOnSave(existing, request);
    }
}
```

### 3.4 物料主服务核心调度编排（`MaterialServiceImpl`）

在 `MaterialServiceImpl` 中，通过 `MaterialCodeGeneratorFactory` 实现新增发号与更新不可变性的统一生命周期编排：

```java
@Override
public MaterialResponse createMaterial(MaterialRequest request) {
    // 1. 外键合法性校验 (分类、类型等基础字典)
    validateForeignKeys(request);

    // 2. 解析分类生效策略
    EffectiveCodingPolicy policy = materialCategoryService.resolvePolicy(
            request.getMaterialCategoryId(), CurrentTenant.tenantId());
    MaterialCodeGenerator generator = generatorFactory.getGenerator(policy.getStrategy());

    // 3. 策略前置校验 (existing 为 null)
    generator.validate(null, request, policy);

    // 4. 分配正式物料编码 (SEQUENTIAL 自动算号、FASHION 拼公式、MANUAL 原样使用)
    String allocatedCode = generator.allocateCode(request, policy);
    request.setCode(allocatedCode);

    // 5. 编码全局防重与落库
    if (materialRepository.existsByCode(allocatedCode)) {
        throw new ValidationException("validation.material.code.duplicate");
    }

    Material entity = materialMapper.toEntity(request);
    applyDefaultValuationClass(entity, request);
    if (entity.getLowLevelCode() == null) {
        entity.setLowLevelCode(0);
    }
    entity = materialRepository.saveAndFlush(entity);
    return materialMapper.toResponse(entity);
}

@Override
public MaterialResponse updateMaterial(Long id, MaterialRequest request) {
    Material entity = materialRepository.findById(id)
            .orElseThrow(() -> new NotFoundException("validation.common.not.found"));

    // 1. 物料编码不可变性校验 (严禁改号，入参为空时沿用原号，绝对不重新发号)
    if (StringUtils.hasText(request.getCode()) && !entity.getCode().equals(request.getCode())) {
        throw new ValidationException("validation.material.code.immutable");
    }
    request.setCode(entity.getCode());

    // 2. 外键校验与分类策略解析
    validateForeignKeys(request);
    EffectiveCodingPolicy policy = materialCategoryService.resolvePolicy(
            request.getMaterialCategoryId(), CurrentTenant.tenantId());
    MaterialCodeGenerator generator = generatorFactory.getGenerator(policy.getStrategy());

    // 3. 执行策略更新校验 (传入 existing = entity，校验维度不可变性并排除自身)
    generator.validate(entity, request, policy);

    // 4. 实体更新与持久化 (不调用 allocateCode)
    materialMapper.updateEntity(request, entity);
    entity.setProductModelId(request.getProductModelId());
    entity.setColorId(request.getColorId());
    entity.setProductSizeId(request.getProductSizeId());
    applyDefaultValuationClass(entity, request);

    entity = materialRepository.saveAndFlush(entity);
    return materialMapper.toResponse(entity);
}
```

---

## 4. 前端重构方案（MaterialForm 与 MaterialCategoryForm）

利用本次 V2 重构契机，彻底解决前述 `MaterialForm.tsx` 831 行过长、逻辑混杂的问题，并为分类抽屉增加策略配置能力：

### 4.1 物料分类配置抽屉增强（`MaterialCategoryForm.tsx`）
* **路径**：`src/pages/mm/material-categories/components/MaterialCategoryForm.tsx`
* **新增配置项**：
  1. **编码策略（`codeStrategy`）**：Select 下拉，可选 `FASHION_VARIANT`（款色码变体）、`SEQUENTIAL`（序列流水号）、`MANUAL`（手工录入）；允许清空表示「继承父级」；
  2. **编码前缀（`codePrefix`）**：Text 输入框，仅在策略为 `SEQUENTIAL`（或继承该策略）时展示；提示「留空则默认使用分类代码」；
  3. **流水号长度（`seqLength`）**：Digit 输入框，默认 5；
  4. **共用父级流水号池（`isSeqShared`）**：Switch 开关，仅对子分类可见，开启后号码与策略源大类合并发号。

### 4.2 物料表单抽离与架构解耦（`MaterialForm`）

```text
src/pages/mm/materials/components/MaterialForm/
├── index.tsx                         # 抽屉容器与主表单骨架 (~150 行)
├── hooks/
│   ├── useCategoryCodingPolicy.ts    # 分类策略异步解析与缓存
│   └── useMaterialVariantSuggest.ts  # 抽离款色码变体预览与建议状态机
└── fields/
    ├── TypeInfoFields.tsx            # 类型、分类、科目选择
    ├── VariantFields.tsx             # 款式、颜色、尺码（仅在 FASHION_VARIANT 时渲染）
    └── BasicInfoFields.tsx           # 编码（依据策略动态只读/手填）、名称
```

---

## 5. 质量保证与测试用例矩阵

| 用例编号 | 测试场景 | 前置配置 | 操作步骤 | 预期结果 |
| :---: | :--- | :--- | :--- | :--- |
| **TC-01** | 服装类变体物料新增 | 分类 `APP` 设为 `FASHION_VARIANT` | 选择分类 `APP`，选款号、颜色、尺码 | 自动提示建议编码 `A12345-A01`，保存成功且流水落库。 |
| **TC-02** | 原料类流水物料新增 | 分类 `FAB` 设为 `SEQUENTIAL` | 选择分类 `FAB`，填名称与单位，`code` 留空保存 | 款色码区域自动隐藏，保存后自动生成 `FAB-00001`。 |
| **TC-03** | 标品配件免款式生成 | 分类 `BAG`（成品类型）设为 `SEQUENTIAL` | 选择分类 `BAG`，保存 | 不强制要求款号与尺码组，成功以流水号形式生成成品物料。 |
| **TC-04** | 客供定制料号录入 | 分类 `OEM` 设为 `MANUAL` | 选择分类 `OEM`，手动输入 `NIKE-123` 并保存 | 界面开放输入，后端校验唯一性后原样存入。 |
| **TC-05** | 分类树策略多级继承 | 顶级 `A` 设为 `SEQUENTIAL`，子级 `A-1` 未设 | 选择子级 `A-1` | 自动向上识别为 `SEQUENTIAL` 策略。 |
| **TC-06** | 子分类特殊策略覆盖 | 顶级 `A` 为 `FASHION_VARIANT`，子级 `A-SP` 显式设为 `MANUAL` | 选择子级 `A-SP` | 成功覆盖父级策略，表单开放手工录入，不强制款色码。 |
| **TC-07** | 非变体分类防呆拦截 | 分类 `FAB`（`SEQUENTIAL`） | 试图在「组合生成」向导中选用此分类 | 向导明确提示「非变体分类无需矩阵生成」，拦截非法操作。 |
| **TC-08** | 多分类流水物理隔离 | 面料 `FAB` 与辅料 `ACC` 均设为 `SEQUENTIAL` | 先建一笔 `FAB` 物料，再建一笔 `ACC` 物料，再建一笔 `FAB` 物料 | `FAB` 生成 `FAB-00001` 与 `FAB-00002`，`ACC` 生成 `ACC-00001`，号段完全隔离连续。 |
| **TC-09** | 子分类共用父级流水池 | 父类 `FAB`（SEQUENTIAL），子类 `FAB-KNIT` 设 `is_seq_shared=true`（未配 prefix） | 分别在父类和子类下建料，`code` 留空保存 | 统一累加 `MATERIAL_CAT:FAB_ID` 流水池，前缀统一继承号池属主，号码依次为 `FAB-00001`, `FAB-00002`。 |
| **TC-10** | OEM/代工场景同款色码不同物料编号共存 | 分类 `OEM` 设为 `MANUAL` | 建立两笔物料，均选择同一款号与尺码 40，编码分别为 `NIKE-40` 与 `ADI-40` | 两笔均成功保存，不再被 V1 的物理部分唯一索引拦截。 |
| **TC-11** | 变体策略下相同款色码依然受业务拦截 | 分类 `APP` 设为 `FASHION_VARIANT` | 手工试图建两笔同款同色同码物料 | 受到 `FashionVariantCodeGenerator` 中的 `assertVariantUnique` 拦截并提示已存在。 |
| **TC-12** | SEQUENTIAL 空 Code 正常通过校验 | 分类 `FAB` 设为 `SEQUENTIAL` | POST `/api/mm/materials` 请求中 `code` 为 null 或空字符串 | 通过 Bean Validation，后端发号器生成 `FAB-00001`，响应实体中返回正式编码。 |
| **TC-13** | 批量生成接口防呆强拦截 | 分类 `FAB`（`SEQUENTIAL`） | 调用 POST `/api/mm/materials/variant-generate`，传入该分类 ID | 后端直接拦截并抛出 400（`validation.material.variant.generate.notFashionVariantCategory`）。 |
| **TC-14** | 子分类继承策略但使用自身代码作为默认前缀 | 顶级 `FAB`（SEQUENTIAL，未配 prefix），子级 `KNIT`（未配 strategy 与 prefix，独立号池） | 在子级 `KNIT` 下建料 | 解析前缀为 `KNIT`，号池独立，生成 `KNIT-00001`。 |
| **TC-15** | 物料编码不可变性校验 | 任何已有物料 | 调用 PUT `/api/mm/materials/{id}` 试图将原 `code` 改为新值 | 后端直接拒绝，抛出物料编码不可修改业务异常。 |
| **TC-16** | 复制物料多策略适配 | 已有 `SEQUENTIAL` 物料 `FAB-00005` | 点击列表行「复制」按钮 | 前端抽屉打开，`code` 自动清空且为只读占位符；保存时生成全新递增流水号 `FAB-00006`。 |
| **TC-17** | 编码总长超长安全拦截 | 分类前缀配置 48 个字符，流水 5 位 | 触发流水发号 | 最终编码超过 50 位，发号器抛出 `validation.material.code.overflow` 异常。 |
| **TC-18** | 更新物料时空 Code 保持原编码免发号 | 已有物料（编码为 `FAB-00001`） | 调用 PUT `/api/mm/materials/{id}`，入参中 `code` 为空，修改名称后保存 | 实体 `code` 严格保持 `FAB-00001`，绝不触发发号器，号池不递增、不产生断号。 |
| **TC-19** | 子分类显式设置 SEQUENTIAL 且共用号池 | 父类 `FAB`，子类 `KNIT` 显式设置 `code_strategy = SEQUENTIAL` 且 `is_seq_shared = true` | 在子类 `KNIT` 下建料 | 号池属主正确锚定父级 `FAB`（`seqScopeCategoryId = FAB_ID`），成功共享父级流水。 |
| **TC-20** | FASHION_VARIANT 变体查重不被 MANUAL 客供料阻断 | 已存在 OEM 分类（MANUAL）物料，填入了款号 M1、尺码 S40，料号为 `NIKE-40` | 在成衣分类（`FASHION_VARIANT`）下为款号 M1、尺码 S40 新增变体物料 | 保存成功，FASHION_VARIANT 的 `assertVariantUnique` 查重范围仅限于变体品类，客供料号不阻断自制品类变体新增。 |
