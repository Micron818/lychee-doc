# 截图资产

按模块分子目录存放训练截图（中越文档共用同一套图）。

建议子目录：`adm` / `basis` / `mm` / `sd` / `scm` / `wm` / `pp` / `fi` / `report` / `common`。

命名：英文小写 + 连字符，例如 `scm/po-print-toolbar.png`。

规范详见 [`../../00-conventions.md`](../../00-conventions.md) 第 5 节。
